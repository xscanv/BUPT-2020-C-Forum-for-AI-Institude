#include<iostream>
using namespace std;
int main()
{
    int year;
    cin >> year;
    if (year % 400 == 0)
        cout << "是";
    else if (year % 100 == 0)
        cout << "不是";
    else if (year % 4 == 0)
        cout << "是";
    else
        cout << "不是";
    return 0;
}
